# Rust
/target/
**/*.rs.bk
*.pdb

# Generated environments and seeds are per-demo artifacts, not source.
# Archived environments belong in the console's archive path, not the repo.
/environments/
*.local.yaml

# Netdata claim credentials must never be committed.
claim_token
claim.conf
.env
